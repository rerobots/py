[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rerobots"
dynamic = ["version"]
requires-python = ">=3.8"

[project.optional-dependencies]
test = [
    "coverage",
    "pylint",
    "pytest",
    "responses",
    "tox",
]
